import XCTest
@testable import DistributePackage

final class DistributePackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(DistributePackage().text, "Hello, World!")
    }
}
